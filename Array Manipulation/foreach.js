const letters = ['a', 'b', 'c', 'd'];

// First for each
letters.forEach(element => console.log(`For Each:  ${element}`));

// Normal for example
for(let i=0; i < letters.length; i++){
    console.log(`Normal For: ${letters[i]}`);
}